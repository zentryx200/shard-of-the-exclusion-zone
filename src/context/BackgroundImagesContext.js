import { createContext } from "react";
import backgroundImages from "../shared/data/background";

const BackgroundImagesContext = createContext(backgroundImages);

export default BackgroundImagesContext;
